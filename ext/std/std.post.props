<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="12.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!--desktop-->
  <PropertyGroup Condition="'$(TargetProject)' == '$(DesktopProjectId)'">
    <TargetsFile>$(MSBuildToolsPath)\Microsoft.CSharp.targets</TargetsFile>
  </PropertyGroup>

  <!--portable-->
  <PropertyGroup Condition="'$(TargetProject)' == '$(PortableProjectId)'">
    <TargetsFile>$(MSBuildExtensionsPath32)\Microsoft\Portable\$(TargetFrameworkVersion)\Microsoft.Portable.CSharp.targets</TargetsFile>
  </PropertyGroup>

  <!--pack-->
  <PropertyGroup Condition="'$(TargetProject)' == '$(PackProjectId)'">
    <TargetsFile>$(ExtDir)nuget.pack.targets</TargetsFile>
  </PropertyGroup>

  <!--unpack-->
  <PropertyGroup Condition="'$(TargetProject)' == '$(UnpackProjectId)'">
    <TargetsFile>$(ExtDir)nuget.unpack.targets</TargetsFile>
  </PropertyGroup>

</Project>